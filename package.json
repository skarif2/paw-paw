{
  "name": "paw-paw-roster",
  "version": "1.0.0",
  "description": "Team roster management for Craftsmen: Saga Team",
  "scripts": {
    "login": "clasp login",
    "build": "tsc && cp src/appsscript.json dist/appsscript.json",
    "push": "pnpm run build && clasp push"
  },
  "devDependencies": {
    "@google/clasp": "^3.2.0",
    "@types/google-apps-script": "^1.0.83",
    "typescript": "^5.7.0"
  }
}